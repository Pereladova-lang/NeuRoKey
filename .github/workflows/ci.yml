name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: neurokey
          POSTGRES_PASSWORD: neurokey_dev
          POSTGRES_DB: neurokey
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U neurokey"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: "postgresql://neurokey:neurokey_dev@localhost:5432/neurokey"
      AUTH_SECRET: "ci-test-secret"
      BILLING_MOCK: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm test --workspace=packages/neurokey-core
      - run: npx prisma migrate deploy
        working-directory: neurokey-pilot
      - run: npx prisma db seed
        working-directory: neurokey-pilot
      - run: npm run lint --workspace=neurokey-pilot
      - run: npx vitest run
        working-directory: neurokey-pilot
